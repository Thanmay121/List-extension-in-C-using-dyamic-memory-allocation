# List-extension-in-C
right now present as a stand off program,has append function currently.
Paste/clone the code into a file and include the file using 
#include "pylist.c"
and use a pointer pointing to your variable using the list() function which has two parameters, list("lenghtoflist","elements of list")
append(list_name,element)
